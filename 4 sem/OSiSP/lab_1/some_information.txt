Все лабораторные требуется сразу компилироать в терминале, а после запускать в нем же. Пример:

1) скачиваем расширения для C++:

  sudo apt install g++
  
2) компилируем файл:

  g++ lab_1 -o try.cpp
  
3) после запускаем скомпилированный файл:

  ./lab_1
